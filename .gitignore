# Logs written by the Firebase local test copy
*-debug.log

# Private test photos, never published
dev-local/

# The relay's local test state and local secrets (relay/psa-prices.js)
relay/.wrangler/
relay/.dev.vars
